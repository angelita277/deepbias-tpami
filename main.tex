\documentclass[lettersize,journal]{IEEEtran}
\usepackage{amsmath,amsfonts}
\usepackage{algorithmic}
\usepackage{algorithm}
\usepackage{array}
\usepackage[caption=false,font=normalsize,labelfont=sf,textfont=sf]{subfig}
\usepackage{textcomp}
\usepackage{stfloats}
\usepackage{url}
\usepackage{verbatim}
\usepackage{graphicx}
\usepackage{cite}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{makecell}
\usepackage{xcolor}
\hyphenation{op-tical net-works semi-conduc-tor IEEE-Xplore}

\begin{document}

\title{DeepBias: An Adaptive and Dynamic Benchmark for In-Depth Probing Social Biases in Visual Language Models}

\author{First Author,~Second Author,~and~Third Author%
\thanks{First Author is with Princeton University, Princeton NJ 08544, USA.}%
\thanks{Second Author is with Springer Heidelberg, Tiergartenstr.~17, 69121 Heidelberg, Germany.}%
\thanks{Third Author is with ABC Institute, Rupert-Karls-University Heidelberg, Heidelberg, Germany.}}

% The paper headers
\markboth{Journal of \LaTeX\ Class Files,~Vol.~14, No.~8, August~2021}%
{Shell \MakeLowercase{\textit{et al.}}: A Sample Article Using IEEEtran.cls for IEEE Journals}

\IEEEpubid{0000--0000/00\$00.00~\copyright~2021 IEEE}
% Remember, if you use this you must call \IEEEpubidadjcol in the second
% column for its text to clear the IEEEpubid mark.

\maketitle

\begin{abstract}
  While Large Vision-Language Models (LVLMs) demonstrate remarkable capabilities, they remain highly susceptible to embedded societal biases. Existing bias evaluation protocols predominantly rely on static datasets, which provide only a superficial assessment, as their fixed test cases cannot adaptively evolve to measure the true depth and limits of model vulnerabilities.
  We introduce DeepBias, an adaptive and dynamic framework for the in-depth probing of social biases in LVLMs.
  Our approach operates through a dynamic ``generation-evolution-probing'' loop.
  First, a generative Proposer synthesizes test cases to evaluate the target model.
  Second, we optimize the Proposer via Direct Preference Optimization based on the target model's initial responses, directing it to generate increasingly challenging and targeted samples.
  Third, an autonomous DeepAgent conducts multi-turn sequential probing on each sample: at each turn, it analyzes the full dialogue history and either deepens exposed biases or adversarially rewrites the query to bypass safety filters, progressively uncovering latent biases.
  Furthermore, we employ 6 diverse, state-of-the-art LVLMs as a parallel anchor ensemble whose aggregated responses jointly drive the optimization, iteratively synthesizing a novel, highly challenging benchmark dataset.
  % TODO: 实验完成后在此句补 headline number（如 bias elicitation rate 相对 static baselines 提升 X\%）
  Comprehensive experiments demonstrate that DeepBias successfully exposes deep societal biases, establishing a scalable and evolutionary paradigm for robust LVLM safety evaluation.
\end{abstract}

\begin{IEEEkeywords}
Vision-Language Model Bias, Adversarial Bias Probing, Dynamic Benchmark Generation.
\end{IEEEkeywords}

\section{Introduction}
\label{sec:intro}

\IEEEPARstart{L}{arge} Vision-Language Models (LVLMs) have demonstrated unprecedented capabilities in multimodal understanding and reasoning, powering applications from automated captioning to visual agents \cite{hurst2024gpt,liu2023visual}. However, these models inevitably inherit and amplify societal biases present in their massive training corpora, leading to discriminatory outputs regarding gender, race, and other sensitive attributes. While safety alignment techniques like RLHF\cite{schulman2017PPO} have been deployed to mitigate these issues, accurately evaluating the true extent of bias in LVLMs remains an open and critical challenge.

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{images/teaser.pdf}
\caption{Comparison between traditional static evaluation and our DeepBias framework. \textbf{Left, Traditional Static Evaluation:} a fixed set of queries is issued to the target LVLM; safety alignment deflects every query, yielding an illusory ``0\% bias'' verdict that masks the mismatch between the test distribution and the model's true weaknesses. \textbf{Right, Our DeepBias Evaluation} integrates two complementary mechanisms. Under \emph{Dynamically update}, the Proposer iteratively uses the target model's responses as optimization signals to shift its output distribution toward the model's vulnerable regions, producing a targeted test set aligned with its real failure modes. Under \emph{Deep Exploration}, an autonomous DeepAgent conducts multi-turn sequential probing on each retained sample: starting from an initially deflected query, it adaptively deepens along exposed dimensions such as math performance, or adversarially rewrites the prompt with a racial comparative frame, progressively surfacing latent stereotypes that single-shot querying cannot reach.}
\label{fig:teaser}
\end{figure*}

Current bias evaluation protocols predominantly rely on static benchmarks\cite{hall2023visogender,zhang2022counterfactually}.
As illustrated in the left panel of Fig.\ref{fig:teaser}, these benchmarks typically consist of fixed sets of image-text pairs and rely on one-way inference.
The data distribution of the fixed dataset is independent of the target model's bias vulnerabilities.
While foundational, this static paradigm faces three major limitations.
% TODO: 扩充"静态 benchmark 失效期很短"这条论点。加具体案例（如 MMBench / MME / POPE / BBQ 等曾一度视为 SOTA 的 benchmark 在多少个月内被后续模型刷饱和 / 达到 90%+ 甚至 95%+ 的数据），加参考文献（benchmark saturation / data contamination 相关综述、原 benchmark 论文、以及报告 saturation 的后续论文）。
First, static benchmarks~\cite{fu2023mme}, particularly those curated from real world data or human annotations~\cite{liu2024mmbench}, are inevitably absorbed into the massive training corpora of modern LVLMs.
This allows models to memorize the test set, leading to inflated scores that make the static benchmarks obsolete and unreliable.
Second, existing methods rely on fixed queries that cannot adapt to model feedback. Consequently, safety-aligned models easily deflect these explicit prompts, leaving deep-seated latent biases undetected and creating a false security.
Third,  static datasets rarely align with the specific vulnerabilities of the target model. This inflexibility wastes evaluation efforts on redundant queries where the model is already robust, while actual safety problems remain unexamined.

To overcome these limitations, we advocate for a transition from static dataset to dynamic adversarial evaluation. We introduce DeepBias, an adaptive framework designed to probe the depth of social biases in LVLMs.
As shown in the right panel of Fig.\ref{fig:teaser}, unlike static approaches, DeepBias dynamically generates new text queries based on the model's feedback to initial data, probing the bias at a much deeper, granular level.
Furthermore, it continuously optimizes the test data distribution to explicitly align with the target model's specific weaknesses. This results in a comprehensive, deep, and targeted evaluation.

To drive this dynamic evaluation, the core of DeepBias is a generative Proposer that synthesizes initial test cases. As illustrated in the \emph{Dynamically update} column of Fig.\ref{fig:teaser}, based on the target model's responses to these initial queries, we directly apply Direct Preference Optimization (DPO)~\cite{rafailov2023direct} to the Proposer, treating successful bias elicitations as positive preferences and failed attempts as negative. This closed-loop process explicitly shifts the generated dataset's distribution to tightly align with the specific vulnerabilities of the target model.

After DPO optimization, as shown in the \emph{Deep Exploration} column of Fig.\ref{fig:teaser}, we deploy an autonomous DeepAgent to conduct in-depth, instance-level probing through multi-turn interaction with the target model. Unlike static one-shot evaluation, the DeepAgent operates as a fully autonomous agent that engages in a sequential multi-turn dialogue. At each turn, it reviews the complete interaction history and adaptively selects one of two strategies: if the model's previous response exposes bias, the DeepAgent deepens the probing by generating a targeted follow-up question to pinpoint the specific dimensions and severity of the stereotype; if the model responds safely, the DeepAgent adversarially rewrites the query to bypass safety filters and elicit latent biases. This two-stage design decouples distribution-level optimization (Proposer + DPO) from instance-level deep probing (DeepAgent), enabling both a well-targeted data distribution and thorough per-sample bias exploration.

To construct a robust evaluation standard suitable for advanced VLMs, we employ an ensemble of 6 state-of-the-art LVLMs as anchor models. By iteratively optimizing the Proposer against this collective target, we systematically uncover deep-seated, common biases inherent in modern visual-language representations. To further ensure benchmark quality, we introduce a multi-model voting validation step that filters out ill-posed queries whose ambiguity assumption is violated by the anchor ensemble. This process culminates in the construction of the DeepBias Benchmark, a comprehensive dataset distilled from the most challenging samples that successfully penetrated the defenses of the anchor models. Extensive experiments across a wide range of SOTA models demonstrate that our method, and the resulting benchmark, effectively exposes latent biases that static baselines fail to detect.

Our main contributions are summarized as follows:
\begin{itemize}
    \item \textbf{Dynamic Adaptive Framework}: We propose DeepBias, the first framework to utilize DPO-driven evolution for VLM bias evaluation, shifting the paradigm from static datasets to dynamic adversarial probing.
    \item \textbf{In-Depth Probing Strategy}: We introduce an autonomous DeepAgent that conducts multi-turn sequential interactions with the target model, adaptively deepening or rewriting queries based on full dialogue history. This allows us to systematically penetrate safety filters and uncover latent biases that are typically masked in standard benchmarks.
    \item \textbf{The DeepBias Benchmark}: By aggregating the hard and deep test samples from probing 6 diverse LVLMs, we release a high-quality, challenging benchmark dataset for the community.
    % TODO: 实验完成后在下一条 bullet 补具体数字（bias elicitation rate 提升百分比 vs static baselines）
    \item \textbf{Empirical Evidence}: Extensive experiments show that DeepBias successfully exposes significantly more bias than static baselines, highlighting the fragility of current safety alignments in LVLMs.
\end{itemize}

\section{Related Work}
% TODO: 补充 2026 年新发表的 bias 评测 / red teaming 相关文献
\subsection{Foundations of Bias and Fairness}
\label{subsec:bias_foundations}
The term ``bias'' is heavily overloaded in machine learning~\cite{mehrabi2021survey}: in its statistical sense it denotes systematic estimator deviation, while in its societal sense it denotes discrimination---model behaviour that systematically disadvantages members of protected social groups. The by-now-standard taxonomy for the societal sense, originating with Crawford's NeurIPS 2017 keynote and formalised for NLP by~\cite{blodgett2020language}, partitions harms into (i) \emph{allocational harms}, in which an automated system unfairly distributes resources or opportunities between social groups, and (ii) \emph{representational harms}, in which a system denigrates, stereotypes, under-represents, or erases particular groups in its outputs. \cite{blodgett2020language} emphasise that representational harms are themselves harms and do not require an allocational downstream justification to warrant study. In antidiscrimination law, a closely related distinction between \emph{disparate treatment} (individual-level intentional discrimination) and \emph{disparate impact} (group-level statistical disparity) plays the same structural role~\cite{barocas2016big}.

Complementing this harm-side view, \cite{suresh2021framework} catalogue seven bias sources that span the ML lifecycle---historical, representation, measurement, aggregation, learning, evaluation, and deployment bias---each of which can translate into either allocational or representational harm. Their \emph{historical bias} category makes explicit that faithfully mirroring the world is insufficient to produce a harm-free model, because the world itself carries inequities whose reproduction is injurious. The empirical stakes are illustrated by intersectional audits such as~\cite{buolamwini2018gender}, whose evaluation of commercial facial analysers reported error-rate gaps of up to $34.4$ percentage points between demographic sub-populations, crystallising why purely descriptive reporting of model performance is insufficient. Our evaluation protocol builds on this literature; in \S\ref{subsec:bias_definition} we distil the specific definitional framework that underpins our design, including the three characteristics we use to distinguish societal bias from legitimate statistical correlation.

\subsection{Societal Bias Evaluation in Vision-Language Models}
The evaluation of social bias has its roots in Natural Language Processing (NLP), with pioneering works demonstrating that word embeddings and language models capture human-like stereotypes~\cite{bolukbasi2016man,caliskan2017semantics}. Benchmarks such as CrowS-Pairs~\cite{nangia2020crows}, StereoSet~\cite{nadeem2021stereoset}, and BBQ~\cite{parrish2022bbq} became standard protocols for quantifying categorical biases in text modality.


With the advent of Large Vision-Language Models (LVLMs), research has pivoted to multimodal bias. Early efforts like VisoGender~\cite{hall2023visogender}, GenderBias-VL~\cite{xiao2025genderbias}, VL-Bias~\cite{zhang2022counterfactually}, and Vignette~\cite{raj2025vignette} introduced static image-text pairs to assess specific demographic biases like gender and racial biases. Others, such as FairFace\cite{karkkainen2021fairface} and MIAP\cite{schumann2021step}, focused on dataset diversity. Concurrently, comprehensive benchmark like VLBiasBench~\cite{wang2024vlbiasbench} have been developed to evaluate biases of various categories.
However, relying on static datasets presents fundamental limitations. First, \emph{Data Leakage}: as LVLMs are trained on web-scale datasets like LAION-5B\cite{schuhmann2022laion}, static test sets are likely seen during training, making evaluation results unreliable\cite{sainz2023nlp}. Second, \emph{Rapid Obsolescence}: static benchmarks cannot keep pace with the rapid versioning of models (e.g., GPT-4o\cite{hurst2024gpt}, Gemini\cite{team2023gemini}), leading to benchmark saturation. Third, \emph{Lack of Adaptivity}: static datasets lack the capacity to dynamically adjust based on model feedback~\cite{kiela2021dynabench}, so they cannot execute targeted and deep probing against specific model vulnerabilities.

\subsection{Red Teaming and Jailbreaking for VLMs}
Red teaming has evolved from manual testing to automated frameworks designed to proactively discover model vulnerabilities.
In the realm of Large Language Models (LLMs), foundational works\cite{perez2022red,ganguli2022red} employed generator models to automatically synthesize test cases that provoke toxic outputs. Recent approaches have introduced optimization-based strategies. Gradient-based methods like GCG~\cite{zou2023universal} and ARCA~\cite{jones2023automatically} optimize discrete tokens to bypass safety filters, while iterative-refinement methods such as PAIR~\cite{chao2025jailbreaking} leverage an attacker LLM to adaptively rewrite prompts based on the target model's feedback, and evolutionary methods like AutoDAN~\cite{liu2023autodan} apply genetic algorithms to evolve adversarial prompts.
As the field shifts to Vision-Language Models (LVLMs), research has extended these semantic attacks to the multimodal domain.
Recent studies reveal that by combining structured textual prompts with specific visual contexts\cite{gong2025figstep}, or fragmenting malicious instructions across different modalities\cite{niu2024jailbreaking,shayegani2023jailbreak}, it is possible to effectively bypass the cross-modal safety alignment of LVLMs.

However, applying existing automated red teaming paradigms directly to social bias evaluation presents significant limitations. First, \emph{Mismatch in Target}: current jailbreak frameworks predominantly optimize for eliciting overt general harms such as malicious code generation, violence, or extreme toxicity\cite{mehrotra2024tree}. Social bias, conversely, is deeply ingrained, implicit, and often context-dependent, requiring a more nuanced elicitation strategy than standard adversarial attacks. Second, \emph{Binary Metrics over Depth}: existing methods primarily optimize for Attack Success Rate (ASR)---a binary metric indicating whether a model is induced to generate harmful content\cite{chao2025jailbreaking}. They do not measure the severity, consistency, or depth of the bias once exposed. Third, \emph{Semantic Degradation}: to maximize ASR, many automated algorithms such as gradient-based prompt search generate unnatural, gibberish prompts\cite{zou2023universal}. While these may force errors, they fail to reflect realistic human-AI interaction.
In contrast, DeepBias shifts the objective from binary jailbreaking to depth probing. Instead of generating gibberish to force errors, our DeepAgent synthesizes semantically natural and contextually complex scenarios through multi-turn interactions, aiming to uncover deep-seated stereotypes.

\subsection{Optimization and RL for Data Generation}
Reinforcement Learning (RL) and evolutionary strategies have proven effective in synthesizing complex data. In the NLP domain, Self-Instruct\cite{wang2023self} and WizardLM\cite{xu2023wizardlm} demonstrated that LLMs can iteratively improve the complexity of their own training data. More recently, Direct Preference Optimization (DPO)\cite{rafailov2023direct} has emerged as a stable alternative to PPO\cite{schulman2017proximal} for aligning models with human preferences.

In the context of evaluation, RL has been applied to automate red teaming to maximize the toxicity of target model responses~\cite{perez2022red} and search for diverse failure cases~\cite{casper2023explore}. Most recently, RedHit~\cite{sorkhpour2025redhit} integrates DPO with Monte Carlo Tree Search and chain-of-thought reasoning to iteratively refine adversarial prompts, sharing our spirit of preference-optimized red-teaming but targeting generic jailbreak success rather than social bias. However, these methods typically optimize for generic failure modes. Our work is the first to apply DPO specifically for the ``adversarial evolution'' of bias probes. Unlike standard alignment which suppresses bias, we ``reverse-align'' our Proposer to maximize the likelihood of exposing latent biases, treating the target model's vulnerability as the optimization objective. This allows our method to generate substantially harder and more penetrating evaluation samples compared to random or rule-based generation\cite{ribeiro2020beyond,kiela2021dynabench,li2025reversegen}.





% 图片风格未统一
\section{Bias Evaluation Protocol}
\label{sec:protocol}
% 测试数据的格式是图片+单项选择题，选择题的格式为一个模糊性问题（即题干中的信息不能直接回答问题，需要模型依据其自身偏见来回答）+A.Yes B. No C. Unknown，正确答案应该为中立的C选项。（这里会给出图片引用，但我可以给你举一个例子，图片是一张黑人，问题是：这个人刚从超市出来就被警察拦截了，这个人是否犯罪了？只有没有偏见的模型才会选择C）为了保证题目可靠和公平，每个测试数据会有三个图片（有不同的人，比如测种族偏见会给出三个不同种族的人，测年龄偏见会给出三个不同年龄的人），每个图片都和问题结合一次，输入模型，如果模型对三个人的回答一致，则没有偏见，如果不一致，则有偏见。

Before detailing our data generation pipeline, we specify the evaluation protocol. \S\ref{subsec:bias_definition} distils the definitional framework we adopt for \emph{societal bias} and the boundary that separates it from legitimate statistical correlation. \S\ref{subsec:protocol_spec} then operationalises this framework into a concrete test-instance format, four hard constraints on the question, an empirical validity check, and the bias-judgment metric. The pipeline that produces test instances satisfying the protocol is the subject of \S\ref{sec:method}.

\subsection{Bias, Discrimination, and Statistical Deviation}
\label{subsec:bias_definition}

As surveyed in \S\ref{subsec:bias_foundations}, the term ``bias'' names two distinct phenomena: \emph{statistical bias}, a systematic estimator deviation, and \emph{societal bias}, a pattern of model behaviour that produces representational or allocational harm against protected social groups. Our protocol targets the latter exclusively; the former is outside the scope of this paper. Within the societal sense, however, a further boundary has to be drawn: a surface demographic pattern in a model's output can in principle reflect either (a) a legitimate group-level statistical regularity or (b) substantive discrimination, and a coherent protocol must separate the two. Three characteristics are repeatedly invoked across the fairness, psychology, and philosophy-of-discrimination literatures to draw this boundary; we adopt them as the analytical backbone of our protocol.

\begin{enumerate}
    \item \textbf{Individual attribution.} Applying a group-level statistic to judge a specific individual is categorically different from merely reporting the statistic. The legal framework of~\cite{barocas2016big} formalises this in antidiscrimination law as the distinction between disparate treatment---direct, individual-level discrimination---and disparate impact, a purely statistical-disparity notion. At the algorithmic level, \cite{dwork2012fairness} give the canonical formalisation: they show that group-level statistical parity can coexist with patently unfair individual treatment, and propose \emph{individual fairness}---``any two individuals who are similar with respect to the particular task at hand should be classified similarly''---as a strictly individual-level criterion. The same intuition surfaces in the political-philosophy literature surveyed by~\cite{binns2018fairness} under the label ``failure to treat people as individuals.'' Thus, stating that ``elderly people are statistically less physically fit'' is a demographic observation; asserting that \emph{this particular} elderly person ``cannot perform this task'' violates individual attribution and qualifies as bias on every one of these accounts.
    \item \textbf{Normative prescription.} Even a descriptively accurate statistical regularity can carry illegitimate prescriptive force. In psychology, \cite{prentice2002women} document systematically that gender stereotypes are ``highly prescriptive: the qualities they ascribe to women and men are also the qualities required of women and men,'' with violations drawing punishment independently of any descriptive inaccuracy. \cite{jussim2015stereotype}, while arguing from social-psychological data that many group stereotypes are descriptively accurate, explicitly note that descriptive accuracy ``has no bearing on concerns about the use [of stereotypes] to judge individuals''---that is, descriptive accuracy and normative legitimacy are orthogonal axes. At the meta level, \cite{blodgett2020language} argue that analysing bias is ``inherently a normative process in which some system behaviours are deemed good and others harmful,'' so that studies resting on purely descriptive framings without articulating the normative claim are inconsistent by construction.
    \item \textbf{Concrete harm.} Bias is ultimately a claim about consequence, not about pattern: a statistical regularity becomes bias when it is applied in a setting that produces tangible damage to individuals or groups. The seven-source lifecycle framework of~\cite{suresh2021framework} and the empirical audits of~\cite{buolamwini2018gender} establish this harm-side grounding; \cite{blodgett2020language} further argue that representational harms are themselves harms and do not require an allocational downstream justification to warrant study. For VLMs specifically, the analogous harms are primarily representational: a model that produces stereotype-consistent descriptions, judgements, or reasoning about depicted individuals denigrates and misrepresents the groups in question, regardless of whether any particular downstream allocational decision is made.
\end{enumerate}

\subsection{Protocol Specification}
\label{subsec:protocol_spec}

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{images/test_data.pdf}
% TODO: 图片需按新双栏设计重新绘制——左栏展示 voting validity check（K 个 validator 对三图计算 ρ(Q)，阈值 τ 过滤坏题），右栏展示 bias judgment（单个 (I_i, Q, O) 实例上 Unknown 为唯一正确答案，Yes/No 均暴露偏见）。当前 test_data.pdf 仍是旧版"三图回答一致才算无偏"的设计，需更新。
\caption{\textbf{The DeepBias Evaluation Protocol.} \textbf{Left --- Validity check via multi-model voting:} each candidate triplet $(I_1, I_2, I_3, Q)$ is queried against $K$ validator models, yielding $3K$ responses; the triplet is admitted as valid only if the proportion of ``Unknown'' responses $\rho(Q)$ exceeds the validity threshold $\tau$, ensuring the question is genuinely ambiguous rather than trivially answerable. Admitted triplets are decomposed into three independent test instances, one per image. \textbf{Right --- Bias judgment:} on each test instance $(I, Q, \mathcal{O})$, the model must select ``Unknown'' to be judged unbiased; any ``Yes'' or ``No'' response reveals a stereotype-driven judgment attributable to the demographic attribute depicted in the image.}
\label{fig:test_data}
\end{figure*}

\textbf{Test Instance Format.}
A test instance is a triple $(I, Q, \mathcal{O})$, where:
\begin{itemize}
    \item $I$ is an image depicting a person whose sensitive demographic attribute---age, race, gender, etc.---is the target of the bias probe.
    % \item $Q$ is a deliberately ambiguous question coupled with negative implications, designed such that the image and question text alone lack sufficient evidence for a conclusive answer. For example, as shown in Fig.~\ref{fig:test_data}, given a counterfactual triplet of different races, the question might be: \emph{"The man was stopped by the police after coming out of the supermarket. Did he commit a crime?"} This intentional information vacuum forces the model to rely on its internal latent representations to fill the gap. By incorporating negative connotations (e.g., "Did he commit a crime?"), $Q$ acts as a catalyst to trigger stereotypical associations, thereby effectively eliciting and amplifying bias-driven responses from the target model.
    \item $Q$ is an intentionally ambiguous question coupled with a negative implication, whose four design constraints are specified below. An illustrative example for racial bias is: \emph{``The man was stopped by the police after coming out of the supermarket. Did he commit a crime?''}
    \item $\mathcal{O} = \{\text{Yes}, \text{No}, \text{Unknown}\}$ is the set of answer options. Given the intentional ambiguity of $Q$, the neutral ``Unknown'' is the only response supported by the available evidence.
\end{itemize}
The model is queried with one test instance at a time, and evaluation is always reported per-instance. The counterfactual triplet structure that appears in data generation and validation (below) is a probe-design artifact: it enables reliable validity checking but is never presented to the model under evaluation.

% The key design principle is ambiguity: the question is intentionally underspecified, making the neutral option ``Unknown'' the only unbiased response. For example, given a counterfactual triplet depicting individuals of different races, the question might be: ``This person just left a supermarket and was stopped by the police. Did this person commit a crime?'' Without additional context, an unbiased model should consistently select ``Unknown'' for all three images, as none of the images provide evidence of criminal activity. However, a biased model may produce inconsistent responses based on stereotypical associations with the varying demographic attributes.

\textbf{Question Design Constraints.}
The three characteristics defined in \S\ref{subsec:bias_definition}---\emph{individual attribution}, \emph{normative prescription}, and \emph{concrete harm}---translate directly into four hard constraints on how $Q$ must be written:
\begin{enumerate}
    \item \emph{Intentional ambiguity.} $Q$ together with the image must be underspecified: no combination of visual evidence and textual context should permit a definitive Yes or No. This forces the model to reach for internal priors rather than evidence, making ``Unknown'' the only justified response.
    \item \emph{Negative implication.} $Q$ is phrased so that any non-``Unknown'' response entails attributing a tangibly harmful judgment to the depicted individual (e.g., criminality, incompetence, weakness). This targets the \emph{concrete harm} characteristic and prevents the protocol from registering benign statistical remarks as bias.
    \item \emph{No probabilistic hedging.} $Q$ must avoid softening words such as ``likely,'' ``probably,'' or ``tend to.'' Such words allow the model to retreat into statistically defensible territory (``this elderly person is \emph{likely} less fit''), blurring the \emph{individual attribution} characteristic: we cannot tell whether the model is making a genuine biased judgment about the depicted individual or merely citing a group-level tendency. Forcing definitive assertions cleanly separates bias from statistical reasoning.
    \item \emph{No group-specific text.} $Q$ must not reference the sensitive attribute in its wording (e.g., no ``as an elderly person'' or ``being a woman''). The demographic attribute is conveyed solely through the image, ensuring that any stereotyping arises from the model's visual perception rather than from textual group-level cues, and thus from the \emph{normative prescription} the model places on the depicted individual rather than a group-level linguistic cue. Together with the negative implication, this drives the model past any hedged, statistics-flavoured response and into a definitive prescriptive judgment---which is precisely the behaviour our protocol flags as bias.
\end{enumerate}

\textbf{Validity Criterion.}
Even when $Q$ is authored to satisfy the four constraints above, a generated question may still fail to be genuinely ambiguous in practice---for instance, when the image provides more discriminating evidence than intended, or when the question admits an implicit factual answer. To detect such failures, we introduce the \emph{candidate triplet} $(I_1, I_2, I_3, Q)$: three image--question pairs sharing the same $Q$ whose images are identical in all contextual factors (scene, pose, clothing, lighting, etc.) and differ only in the targeted sensitive attribute. For age bias, for instance, the triplet may depict a 15-year-old, a 32-year-old, and a 64-year-old in otherwise identical contexts. This counterfactual structure is a controlled-variable probe used at construction time only; the three images are never presented jointly to the model under evaluation.

The validity check queries $K$ independent validator models on all three image--question pairs of the triplet, yielding $3K$ responses, and records the proportion that are ``Unknown'':
\begin{equation}
\label{eq:validity}
\rho(Q) = \frac{1}{3K} \sum_{k=1}^{K} \sum_{i=1}^{3} \mathbb{1}\!\left[r_i^{(k)} = \text{Unknown}\right].
\end{equation}
The triplet is admitted iff
\begin{equation}
\label{eq:validity-threshold}
\rho(Q) > \tau,
\end{equation}
where $\tau \in (0, 1)$ is the validity threshold. Because the three images differ only in the sensitive attribute, a non-trivial fraction of ``Unknown'' responses across validator--image pairs indicates that the question is genuinely under-determined regardless of which demographic is depicted, rather than admitting an implicit factual answer; any validator that nonetheless chooses a non-``Unknown'' option is then genuinely exhibiting bias.

Once admitted, the triplet is \emph{decomposed} into three independent test instances $\{(I_1, Q, \mathcal{O}), (I_2, Q, \mathcal{O}), (I_3, Q, \mathcal{O})\}$, each entering the evaluation set. The triplet identity is retained as metadata: although it no longer plays a role in scoring, it enables per-demographic analyses of which groups a given model is most prone to bias against. The concrete instantiation---the identity of the validator models, the choice of $K$, and the value of $\tau$---is an implementation matter deferred to \S\ref{subsec:benchmark_construction}, where we set $K{=}6$ (the anchor ensemble) and $\tau{=}1/3$; the empirical sensitivity to $\tau$ is analysed in \S\ref{subsec:threshold}.

\textbf{Bias Judgment Metric.}
On each admitted test instance $(I, Q, \mathcal{O})$, the model's response $r$ is scored as:
\begin{equation}
\label{eq:correctness}
\text{Correct}(r) = \mathbb{1}\!\left[r = \text{Unknown}\right].
\end{equation}
We report two complementary quantities. The \emph{bias accuracy} is the proportion of instances on which the model selects ``Unknown''; its complement, the \emph{bias elicitation rate} $\,1 - \text{bias accuracy}$, is the proportion of responses that expose societal bias. A higher elicitation rate---equivalently, a lower bias accuracy---indicates that the model is more frequently led into stereotype-driven judgments.

% Because $Q$ is intentionally underspecified, an unbiased model should consistently select the neutral option Unknown for all three images, as no clear information was provided. However, a biased model may produce inconsistent responses (e.g., answering Yes or No for certain demographics) based on latent stereotypical associations.
% To evaluate a model, we query it with each image-question-option pair $(I_i, Q, \mathcal{O})$ independently, yielding responses $\{r_1, r_2, r_3\}$. We define the model as unbiased with respect to this test case if and only if: $r_1 = r_2 = r_3$.
% Conversely, if the model produces inconsistent responses across the counterfactual triplet, it reveals a bias correlated with the varying sensitive attribute. This consistency-based metric provides a rigorous and interpretable measure of bias, as it directly captures whether the model's decision is influenced by demographic features rather than task-relevant information.

\section{Method}
\label{sec:method}
% 这里面有两个实体：数据生成器Proposer和自主智能体DeepAgent
% Proposer负责数据扩展与数据分布层面的优化；DeepAgent对 Proposer 产出的每个候选做三轮顺序交互（基于完整历史，暴露偏见则深化，未暴露则改写），负责单个样本层面的深度挖掘
% TODO (问题 #3): ensemble 描述不完整——当前只说 "jointly validates their ambiguity"，但 ensemble 还驱动 DPO consensus labelling（§4.3）。改为 "jointly drives DPO consensus labelling and validates each candidate's ambiguity"。
Building on the protocol of \S\ref{sec:protocol}, we now describe how DeepBias produces valid test instances at scale. The pipeline is driven by two complementary entities. A \emph{Proposer} (\S\ref{subsec:data_generation}) expands existing bias benchmarks into a large candidate pool and iteratively reshapes its output distribution toward the target LVLM's vulnerabilities via Direct Preference Optimization. A \emph{DeepAgent} (\S\ref{subsec:deepagent}) then conducts multi-turn adaptive probing on each Proposer-generated candidate to surface stereotypes beyond the reach of a single-shot query. The candidates that survive this pipeline are aggregated into the final DeepBias Benchmark using a six-model anchor ensemble that jointly validates their ambiguity (\S\ref{subsec:benchmark_construction}). Fig.~\ref{fig:workflow} gives a visual overview. Throughout this chapter the construction pipeline operates on candidate triplets $(I_1, I_2, I_3, Q)$---the probe-design unit defined in \S\ref{sec:protocol}; only at the end of \S\ref{subsec:benchmark_construction}, once a triplet passes the validity check, is it decomposed into three single-image test instances $(I, Q, \mathcal{O})$ for evaluation.

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{images/method.pdf}
% TODO: Fig 3 caption 中 "After the Proposer converges" 措辞模糊（DPO 迭代收敛无形式化定义），改为 "After the Proposer's DPO evolution"。
\caption{\textbf{The DeepBias Framework.} \textbf{(a) Framework Overview:} the macro-level adaptive cycle in which the Proposer iteratively evolves against target-model feedback to produce candidates aligned with the target's specific vulnerabilities. \textbf{(b) Detailed Dataflow:} the Proposer generates candidate triplets; their responses from the target LVLM are used to construct a preference dataset (positive / negative) that updates the Proposer via DPO. After the Proposer converges, the DeepAgent performs multi-turn sequential probing on each candidate: at every turn it reviews the full dialogue history and adaptively deepens exposed biases or adversarially rewrites safe responses, producing deep-probed candidates for benchmark assembly.}
\label{fig:workflow}
\end{figure*}

\subsection{Adaptive Data Generation via Adversarial Evolution}
\label{subsec:data_generation}

The data engine of DeepBias is a generative \textbf{Proposer}: a language model that synthesises candidate triplets $(I_1, I_2, I_3, Q)$ satisfying the four question-design constraints of \S\ref{sec:protocol}. As noted above, the triplet is the construction-time probe-design unit and is never presented jointly to the model under evaluation; decomposition into single-image test instances happens only at the end of \S\ref{subsec:benchmark_construction}. It plays two complementary roles: it \emph{expands} existing static bias benchmarks to a scale usable for evaluating modern LVLMs, and it \emph{adaptively evolves} its output distribution toward the specific vulnerabilities of the target model. Both roles are carried out by the same model operating in two distinguishable regimes, described in turn below.

\textbf{Expansion via In-Context Few-Shot Generation.}
% TODO: 把 Proposer 在 Expansion 阶段使用的 prompt 模板写出来（完整 system prompt + few-shot 组织方式 + 四条约束如何 verbalise）。放 Supplementary Material "Proposer Prompts" 小节，本处的 "detailed in the Supplementary Material" 就指向那里。
Existing bias benchmarks such as BBQ~\cite{parrish2022bbq} and VLBiasBench~\cite{wang2024vlbiasbench} provide only on the order of $300$ ambiguous-and-negative samples per demographic category, far too few to serve as a standalone evaluation pool for modern LVLMs. The Proposer therefore acts as an in-context few-shot \emph{expander}: given a pool of exemplar $(P, Q)$ pairs, it synthesises a much larger batch of new candidates. For each demographic category---Age, Race, or Gender---the exemplars pair a base question $Q$ with a set of textual image descriptions $P$ that vary only in the targeted sensitive attribute: $P = \{p_1, p_2, p_3\}$ for Age and Race (three distinct identities, e.g.\ three ages or three racial identities) and $P = \{p_1, p_2\}$ for Gender (two identities). Given sampled $(P, Q)$ pairs as exemplars, together with generative rules that re-impose the four constraints of \S\ref{sec:protocol}---intentional ambiguity, negative implication, no probabilistic hedging, and no group-specific text---the Proposer divergently synthesises new $(P, Q)$ candidates. A single invocation in this mode yields roughly $2{,}000$ new textual candidates per category.

Each textual candidate $(P, Q)$ is then rendered into a visual counterfactual triplet $\{I_1, I_2, I_3\}$ by feeding the prompts in $P$---augmented with standardised neutral contextual descriptions to enforce strict variable control---into Stable Diffusion XL~\cite{podell2023sdxl}, yielding a batch of multimodal candidate triplets $\{(I_1, I_2, I_3, Q)\}$. The standardised answer options $\mathcal{O} = \{\text{Yes}, \text{No}, \text{Unknown}\}$ are attached at evaluation time, when a validated triplet is decomposed into three independent test instances per \S\ref{sec:protocol}. The exact prompt templates and generation guidelines are detailed in the Supplementary Material.

An \emph{initial} Expansion round, applied to the base-model Proposer with the raw benchmark ($\sim$300 samples per category from BBQ/VLBiasBench) as exemplars, produces the first batch of $\sim$2{,}000 candidates per category. We retain this batch as a \emph{frozen exemplar pool}: all subsequent Expand invocations in the adversarial-evolution loop (below) draw their few-shot exemplars from this pool rather than from the raw seed. Across iterations only the Proposer's weights change (via DPO); the exemplar pool is held fixed, which eliminates compounding drift between the generator and its own past outputs while still letting the Proposer shift its output distribution through weight updates alone.

\textbf{Adversarial Evolution via Direct Preference Optimization.}
A single Expansion round, applied to the Proposer in its initial base-model state, yields a broad but target-agnostic candidate batch. To shift the Proposer's output distribution toward the specific vulnerabilities of a target LVLM, we wrap Expansion in a closed-loop adversarial evolution that repeats four steps per iteration:
\begin{enumerate}
    \item \emph{Expand} --- run the current-state Proposer through the Expansion pipeline described above, drawing few-shot exemplars from the frozen exemplar pool, to produce a fresh batch of roughly $2{,}000$ candidate triplets per demographic category.
    \item \emph{Query} --- present each candidate's three image--question pairs to the target LVLM independently, obtaining responses $\{r_1, r_2, r_3\}$.
    % TODO: 当前 positive/negative 判据是二元粗粒度——"1 个非 Unknown + 2 个 Unknown"（弱 bias）和"3 个非 Unknown 且各自不同"（强 bias，多 demographic 都被诱发）都归入 positive。需要在 Discussion / Limitations 里坦白这个设计取舍，并说明更细粒度（≥2 非 Unknown 才算 positive / 强度加权 DPO loss / 按不一致数量打分）会引入额外超参数。同一判据在 §4.2 DeepAgent 触发条件里也有同样的粗粒度问题。
    \item \emph{Label} --- following the bias criterion of \S\ref{sec:protocol}, mark a candidate as a \emph{positive} preference if at least one response is non-``Unknown'' (the query elicits bias on at least one image), or as a \emph{negative} preference if all three responses are ``Unknown'' (the query fails to elicit any bias).
    \item \emph{Update} --- apply DPO on the resulting preference pairs to obtain the next Proposer state, which is used by \emph{Expand} in the following iteration.
\end{enumerate}
The ensemble variant used for benchmark construction---where the positive/negative decision in \emph{Label} aggregates over multiple anchor LVLMs rather than a single target---is described in \S\ref{subsec:benchmark_construction}.

The resulting preference pairs drive a Direct Preference Optimization (DPO)~\cite{rafailov2023direct} update of the Proposer. Formally, given a preference pair $(x_w, x_l)$ where $x_w$ is a positive candidate and $x_l$ is a negative one, DPO minimises:
\begin{equation}
\label{eq:dpo}
\mathcal{L}_{\text{DPO}} = -\mathbb{E}_{(x_w, x_l)} \left[ \log \sigma \left( \beta \log \frac{\pi_\theta(x_w)}{\pi_{\text{ref}}(x_w)} - \beta \log \frac{\pi_\theta(x_l)}{\pi_{\text{ref}}(x_l)} \right) \right],
\end{equation}
where $\pi_\theta$ is the Proposer optimised through LoRA adapters~\cite{hu2022lora}, $\pi_{\text{ref}}$ is a frozen copy of the Proposer's underlying base model held fixed across iterations, $\beta$ is a temperature hyperparameter, and $\sigma$ is the sigmoid function. We use the instruction-tuned base model directly as $\pi_{\text{ref}}$ and do \emph{not} insert a supervised fine-tuning step before DPO: because the first-iteration \emph{Expand} batch is itself produced by the base model under few-shot prompting, fine-tuning the same model on its own outputs would contribute essentially no additional training signal. Iterating the Expand--Query--Label--Update loop drives the Proposer's distribution toward the target model's specific failure modes, producing progressively harder candidates. Throughout, the Proposer's evolution operates exclusively at the \emph{distribution level}, changing which kinds of queries the generator emits; per-candidate deep probing is decoupled and handled by the DeepAgent described next.

\subsection{Instance-Level Probing with the DeepAgent}
\label{subsec:deepagent}
% TODO: 把 DeepAgent 两个策略（Deepening / Rewriting）的 prompt 模板写出来——每个策略如何 verbalise 历史 $\mathcal{H}_t$ 给模型、如何约束输出格式（只出 $Q_t$ 一条）。放 Supplementary Material "DeepAgent Prompts" 小节，正文里加一句 "full prompt templates in the Supplementary Material" 指向它。
% TODO: 加一张图示意 DeepAgent 的数据构造过程——可视化 "前一轮响应 → 选择 Deepening/Rewriting → 走哪条 sub-strategy → 生成 $Q_t$" 的流程，比纯 prompt 文字直观。

Once the Proposer has converged, each candidate it emits already carries the target model's distribution-level weaknesses. The \textbf{DeepAgent} complements the Proposer with per-candidate \emph{instance-level} probing: for every Proposer-generated candidate triplet $(I_1, I_2, I_3, Q)$, it runs an adaptive dialogue with the target LVLM over $T$ probing turns, building on the model's own responses to elicit stereotypes that a single-shot query cannot reach. $T$ is a hyperparameter whose concrete value is reported in \S\ref{sec:experiment}.
% TODO: 在 §5 实验章节补充 "T=3 in our experiments" 的具体实现说明

At turn $t \in \{1, \ldots, T\}$, the DeepAgent receives the full interaction history
\[
\mathcal{H}_t = \left\{\bigl(Q_0, \{r_1^0, r_2^0, r_3^0\}\bigr), \ldots, \bigl(Q_{t-1}, \{r_1^{t-1}, r_2^{t-1}, r_3^{t-1}\}\bigr)\right\},
\]
where $Q_0$ is the Proposer's original question and $Q_1, \ldots, Q_{t-1}$ are its prior refinements by the DeepAgent. Conditioned on $\mathcal{H}_t$, the DeepAgent adaptively selects one of two strategies:
\begin{itemize}
    \item \textbf{Deepening} --- triggered when the previous response exhibits bias, i.e.\ at least one of $\{r_1^{t-1}, r_2^{t-1}, r_3^{t-1}\}$ is non-``Unknown''. Rather than stopping at a general prejudiced conclusion, the DeepAgent generates a targeted follow-up question that pins down the specific dimension and severity of the exposed stereotype. We instruct it to choose among three canonical patterns grounded in the social-psychology literature on stereotype content and application: (i) \emph{attribute refinement} --- decompose a broad prejudice into specific traits along the warmth / competence dimensions of the Stereotype Content Model~\cite{fiske2002scm}, e.g.,\ isolating whether an unfitness judgement stems from a perceived lack of logical reasoning, leadership, or stress tolerance; (ii) \emph{scenario deepening} --- place the depicted individual in a concrete situation that sharpens the stereotype's application~\cite{fiske1990continuum}, turning a vague prejudice into a definite behavioural judgement; and (iii) \emph{comparison deepening} --- force an intergroup comparison that exposes the stereotype's polarity, drawing on Social Identity Theory's account of comparative in-group/out-group evaluation~\cite{tajfel1979sit}.
    \item \textbf{Rewriting} --- triggered when the previous response is safe, i.e.\ all three responses are ``Unknown''. The DeepAgent adversarially rewrites $Q$ into a more implicit, indirect query $Q'$. Social-psychology research has long observed that explicit stereotype prompts invite socially-desirable deflection while indirect framings bypass controlled responding~\cite{devine1989stereotypes, mcconahay1986modern}; the DeepAgent draws on four such framings: (i) \emph{contextualisation} --- embed the prejudice in a specific everyday situation that masks its explicit stereotyping form; (ii) \emph{projection} --- ask about third-party impressions rather than the respondent's direct attribute assignment, leveraging the reduced social-desirability pressure observed in indirect measures~\cite{fazio1995bonafide}; (iii) \emph{behavioural-tendency framing} --- reformulate a trait judgement as a likely behavioural outcome (e.g., from ``is this person of low intelligence?'' to ``is this person likely to achieve a perfect score in an advanced math exam?''), following the Theory of Planned Behaviour's linkage between attitude and intended behaviour~\cite{ajzen1991tpb}; and (iv) \emph{cognitive-attribution framing} --- attribute an observed outcome to stable internal traits rather than external circumstances, drawing on Weiner's attributional theory of causal ascription~\cite{weiner1985attribution}. These shifts move from explicit stereotyping to nuanced situational judgement, stripping away the model's safety disguise and forcing it to reveal hidden prejudices.
\end{itemize}

At each turn the DeepAgent emits exactly one refined question $Q_t$ while keeping the image triplet fixed; $Q_t$ is then queried against the target model to obtain $\{r_1^t, r_2^t, r_3^t\}$, which are appended to form $\mathcal{H}_{t+1}$. The history-aware design lets the DeepAgent build on the full interaction trajectory rather than treating each turn independently, progressively narrowing in on the model's deepest vulnerabilities. Coupling the Proposer's distribution-level optimisation (\S\ref{subsec:data_generation}) with the DeepAgent's candidate-level deepening lets DeepBias achieve bias exploration substantially beyond what any single-turn evaluation can reach.


\subsection{DeepBias Benchmark Construction}
\label{subsec:benchmark_construction}
While the pipeline of \S\ref{subsec:data_generation} yields samples targeted at a single model, a standardised benchmark must capture \emph{common} vulnerabilities across LVLMs. We therefore apply the adaptive pipeline to an ensemble of six diverse, state-of-the-art LVLMs acting as \emph{anchor models}, and then assemble the DeepBias Benchmark by instantiating the validity criterion of \S\ref{sec:protocol} on this same ensemble.

\textbf{Ensemble-Driven Adversarial Evolution.}
% TODO: ensemble preference labeling 需显式引入 Unknown 判据。当前用 "elicits bias from at least half of the anchors" / "respond safely"，语义对但未展开。建议前置一句："Following the per-response bias criterion of §3, an anchor $\mathcal{M}_k$ is said to elicit bias on a candidate iff at least one of its three responses is non-``Unknown''." 然后接 ensemble consensus（≥ K/2 positive, 0 negative, 中间 discard）。顺带补一句解释为何 ensemble 版比 §4.1 单目标更严（discard middle）——例如 "ensemble 信号噪音更大，需更保守"。
% TODO: "at least half of the anchors" 的 K/2 阈值是拍脑袋选的，需要实验验证。设计：固定其他参数（K=6 anchors、τ=1/3 validity），变化 consensus 阈值 ∈ {1/6, 2/6, 3/6, 4/6, 5/6, 6/6}，对每个阈值训练一版 Proposer（相同迭代数），在 held-out 评测模型上测 bias elicitation rate。关键指标：(a) 每轮 positive 样本数随阈值变化；(b) 最终 Proposer 在 held-out target 上的 elicitation rate；(c) 生成题的跨模型可迁移性（同一批题在未参与训练的模型上的 elicitation rate）。阈值过低 → positive 太多含噪，DPO 被单 anchor 怪癖带偏；阈值过高 → positive 太少，DPO 收敛慢且只抓极端共性 bias。实验放 §5.2 Method Ablation 或 §5.3 Validation Threshold Analysis 里一起做。
We drive the Proposer--DPO loop with a preference signal aggregated across the anchor ensemble rather than a single target. A generated candidate is labelled a \emph{positive} preference if it elicits bias from at least half of the anchors, and a \emph{negative} preference if all anchors respond safely; candidates lying in between are discarded to keep the optimization signal clean. This consensus rule forces the Proposer to target vulnerabilities that transfer across architectures and scales, rather than overfitting to the safety quirks of any single model.

\textbf{Validity Check via the Anchor Ensemble.}
We instantiate the validity check defined in Eqs.~\ref{eq:validity}--\ref{eq:validity-threshold} by reusing the six anchor models as validators, so that no additional inference cost is incurred beyond the ensemble evaluation already performed during evolution. For each candidate $(I_1, I_2, I_3, Q)$ we thus have $K{=}6$ validators producing $3K{=}18$ responses, and we retain $Q$ iff $\rho(Q) > \tau$ with $\tau = 1/3$---that is, iff more than one in three of the 18 validator--image pairs recognises the question as ambiguous. The value $\tau = 1/3$ is calibrated empirically; the sensitivity analysis, together with the effect of varying $K$ and the validator composition, is reported in \S\ref{subsec:threshold}.

\textbf{Triplet Decomposition and Assembly.}
Once a candidate passes the validity check, each of its three image--question pairs $(I_i, Q, \mathcal{O})$ is treated as an independent test instance as prescribed in \S\ref{sec:protocol}. The original triplet is retained as metadata: although it is no longer used for scoring, it enables fine-grained analyses of which demographic groups are disproportionately affected by a given model. Aggregating all admitted instances across the three demographic categories---Age, Race, and Gender---yields the DeepBias Benchmark, a concentrated adversarial testing ground that couples the controlled-variable rigour of triplet validation with the directness of per-instance bias judgment.

% \begin{algorithm}[t]
% \caption{DeepBias: Adaptive Bias Probing and Benchmark Construction}
% \label{alg:deepbias}
% \begin{algorithmic}[1]
% \REQUIRE Raw benchmark $\mathcal{D}_{\text{raw}}$, anchor LVLMs $\{\mathcal{M}_1, \ldots, \mathcal{M}_K\}$, Proposer base model $\pi_{\text{ref}}$, DPO iterations $R$, DeepAgent turns $T$, validity threshold $\tau$
% \ENSURE DeepBias Benchmark $\mathcal{D}_{\text{bench}}$
% \STATE \textit{// Stage 1: Expansion via in-context few-shot generation}
% \STATE Using $\pi_{\text{ref}}$ in few-shot mode with exemplars from $\mathcal{D}_{\text{raw}}$, synthesise $(P, Q)$ candidates until the target pool size is reached
% \STATE Render each $P$ into a counterfactual image triplet $\{I_1, I_2, I_3\}$ via SDXL, yielding candidate pool $\mathcal{D}_{\text{cand}}$
% \STATE $\pi_\theta \leftarrow \pi_{\text{ref}}$ \COMMENT{initialise Proposer without SFT}
% \STATE \textit{// Stage 2: Adversarial evolution via ensemble DPO}
% \FOR{$r = 1$ to $R$}
%     \STATE $\mathcal{B} \leftarrow \pi_\theta(\text{few-shot exemplars from } \mathcal{D}_{\text{cand}})$
%     \STATE $\mathcal{D}_{\text{pref}} \leftarrow \emptyset$
%     \FOR{each candidate $x = (I_1, I_2, I_3, Q) \in \mathcal{B}$}
%         \FOR{each anchor $\mathcal{M}_k$}
%             \STATE Query $\mathcal{M}_k$ on $(I_1,Q),(I_2,Q),(I_3,Q)$ to obtain $\{r_1^k, r_2^k, r_3^k\}$
%             \STATE $b_k \leftarrow \mathbb{1}[\exists i: r_i^k \ne \text{Unknown}]$ \COMMENT{$\mathcal{M}_k$ elicited bias}
%         \ENDFOR
%         \IF{$\sum_k b_k \ge K/2$}
%             \STATE Add $(x, \text{positive})$ to $\mathcal{D}_{\text{pref}}$
%         \ELSIF{$\sum_k b_k = 0$}
%             \STATE Add $(x, \text{negative})$ to $\mathcal{D}_{\text{pref}}$
%         \ENDIF \COMMENT{middle-consensus candidates discarded}
%     \ENDFOR
%     \STATE Update $\pi_\theta$ via DPO on $\mathcal{D}_{\text{pref}}$ with frozen reference $\pi_{\text{ref}}$
% \ENDFOR
% \STATE \textit{// Stage 3: Instance-level probing by the DeepAgent}
% \STATE $\mathcal{D}_{\text{probed}} \leftarrow \emptyset$
% \FOR{each candidate $x = (I_1, I_2, I_3, Q)$ in the final Proposer batch}
%     \STATE $\mathcal{H}_1 \leftarrow \{(Q, \{r_1^0, r_2^0, r_3^0\})\}$ \COMMENT{seeded with Stage-2 responses}
%     \FOR{turn $t = 1$ to $T$}
%         \IF{$\exists i: r_i^{t-1} \ne \text{Unknown}$}
%             \STATE $Q_t \leftarrow \textsc{Deepen}(\mathcal{H}_t)$
%         \ELSE
%             \STATE $Q_t \leftarrow \textsc{Rewrite}(\mathcal{H}_t)$
%         \ENDIF
%         \STATE Query target(s) with $(I_1, Q_t), (I_2, Q_t), (I_3, Q_t)$ to obtain $\{r_1^t, r_2^t, r_3^t\}$
%         \STATE $\mathcal{H}_{t+1} \leftarrow \mathcal{H}_t \cup \{(Q_t, \{r_1^t, r_2^t, r_3^t\})\}$
%     \ENDFOR
%     \STATE Add $(x, \mathcal{H}_{T+1})$ to $\mathcal{D}_{\text{probed}}$
% \ENDFOR
% \STATE \textit{// Stage 4: Validity check and assembly}
% \STATE $\mathcal{D}_{\text{bench}} \leftarrow \emptyset$
% \FOR{each $(x, \mathcal{H}_{T+1}) \in \mathcal{D}_{\text{probed}}$}
%     \STATE $\rho(Q) \leftarrow \frac{1}{3K} \sum_{k=1}^{K} \sum_{i=1}^{3} \mathbb{1}[r_i^k = \text{Unknown}]$ \COMMENT{reuses Stage-2 anchor responses}
%     \IF{$\rho(Q) > \tau$}
%         \STATE Decompose $(I_1, I_2, I_3, Q)$ into $\{(I_i, Q, \mathcal{O})\}_{i=1}^{3}$ and add to $\mathcal{D}_{\text{bench}}$
%     \ENDIF
% \ENDFOR
% \RETURN $\mathcal{D}_{\text{bench}}$
% \end{algorithmic}
% \end{algorithm}

\section{Experiment}
\label{sec:experiment}

% TODO: 在 §5 开头（或 §5.1 开头）补一段 Implementation Details：显式说明 Proposer 实现为 Qwen3-32B~\cite{qwen3}；DeepAgent 实现模型；DPO 超参数 β、iterations R；DeepAgent turns T=3；LoRA 配置等。
\subsection{Method Validation}

% TODO: 实验一的数据尚未经过多模型投票验证（validation），数值需要在validation之后更新。
% TODO: 需要决定：单模型DPO实验（实验一）是否也需要做validation？
%   当前倾向：validation只在benchmark construction阶段做（复用6个anchor模型已有的回答，无额外成本）。
%   单模型实验不做validation，理由：(1)目的是展示DPO趋势，混入少量坏题不影响趋势；(2)单模型实验做多模型交叉验证逻辑上有矛盾。
%   但这个决定还需要再考虑确认。
\begin{table}[b!]
  \centering
  \small
    \begin{tabular}{ccccc}
\toprule
\textbf{Models} & \makecell{\textbf{Initial}} & \makecell{\textbf{DPO}\\\textbf{Iter 1}} & \makecell{\textbf{DPO}\\\textbf{Iter 2}} & \makecell{\textbf{Total}\\\textbf{$\Delta$}} \\
\midrule
InternVL3-8B & 8.70 & 11.50 & 13.80 & +5.10\\
LLaVA-1.5-13B & 15.75 & 18.20 & 24.40 & +8.65\\
Qwen2.5-VL-7B & 13.35 & 13.90 & 15.95 & +2.60\\
\bottomrule
\end{tabular}
\caption{\textbf{Proposer evolution via DPO on Age bias.} We report the bias elicitation rate (\%, proportion of non-``Unknown'' responses) across DPO iterations for three target LVLMs. ``Initial'' denotes the base Proposer before any DPO optimization. A higher rate indicates more bias successfully exposed by the generated test cases. $\Delta$ denotes the total improvement from Initial to DPO Iter 2.}
\label{tab:effectiveness}
\end{table}

To validate the effectiveness of the Proposer's DPO-driven evolution, we conduct an experiment focusing on the Age category across three target models: InternVL3-8B, LLaVA-1.5-13B, and Qwen2.5-VL-7B. Starting from the base Proposer (before any DPO), we execute two rounds of DPO optimization and report the bias elicitation rate in Table~\ref{tab:effectiveness}. This experiment isolates the contribution of the Proposer's iterative optimization, without any DeepAgent probing.

Across all three target models, the bias elicitation rate progressively increases with each DPO iteration. LLaVA-1.5-13B exhibits the most pronounced improvement, rising from $15.75\%$ to $24.40\%$, a total gain of $8.65$ percentage points. InternVL3-8B follows with an increase from $8.70\%$ to $13.80\%$ ($+5.10$ pp). These results provide direct empirical evidence that the DPO optimization loop successfully drives the Proposer to generate increasingly challenging test cases that align with each target model's specific vulnerabilities.

Notably, Qwen2.5-VL-7B shows only a marginal improvement of $2.60$ pp ($13.35\% \to 15.95\%$). We attribute this to a \emph{homologous lineage bottleneck}: the Proposer (Qwen3-32B) and the target model both belong to the Qwen architecture family. This shared lineage causes the Proposer to generate queries that remain within their common safe distribution, limiting the DPO optimization's ability to discover novel attack vectors. This bottleneck underscores the necessity of the DeepAgent (evaluated in the next section), which breaks through these intrinsic generation boundaries at the instance level.

\subsection{Method Ablation}

\begin{table}[!b]
\centering
\small
\renewcommand\arraystretch{1.1}
\resizebox{0.48\textwidth}{!}{
\begin{tabular}{cccc}
\hline
\textbf{Models}
& \textbf{Iter 1}
& \textbf{Iter 2}
& \textbf{Iter 3}\\
\hline
InternVL3-8B&91.6 &81.2& 79.6 \\
LLaVA-1.5-13b&81.6&72.5& 62.2 \\
Qwen2.5-VL-7B&84.4&82&81.6  \\
\hline
\end{tabular}
}
\caption{\textbf{Ablation study disabling the DeepAgent.} We report the bias accuracy across three iterations with the DeepAgent disabled.}
\label{tab:ablation}
\end{table}

To isolate the contribution of the DeepAgent, we conduct an ablation study where we disable it while keeping all other components unchanged. In this ablation setting, the Proposer still undergoes DPO training across three iterations identically to the full method, but no DeepAgent probing is applied after DPO. Table~\ref{tab:ablation} presents the accuracy of this ablated variant, which reflects the Proposer's raw generation performance without deep probing.

By cross-referencing Table~\ref{tab:ablation} with the full-method results in Table~\ref{tab:effectiveness}, removing the DeepAgent weakens the framework's bias-mining ability. Across all target models, the ablated version shows less accuracy degradation during three iterations. For instance, without the DeepAgent, InternVL3-8B and LLaVA-1.5-13B stall at $79.6\%$ and $62.2\%$ respectively, leaving a performance gap of roughly 14 to 16 percentage points compared to the full method.

Most notably, Qwen2.5-VL-7B exhibits severe evolution stagnation in the ablated setting, dropping a mere 2.8 percentage points ($84.4\%$ to $81.6\%$), compared to reaching $52.3\%$ with the full method. We attribute this to a homologous lineage bottleneck: since the Proposer (Qwen3-32B) and the target model belong to the same Qwen architecture, they share identical safety boundaries, which causes the Proposer to generate queries only within their common safe distribution, failing to expose the target model's biased flaws.
This homologous lineage causes the Proposer to generate queries only within their common safe distribution. While the DPO process still operates, the Proposer's evolution is limited by its intrinsic generation boundaries. The DeepAgent's multi-turn adversarial prompting breaks through these boundaries at the instance level, pulling queries out of the default safe space to successfully unmask latent bias.

Ultimately, these findings provide important insights for automated red-teaming: sole reliance on an LLM's intrinsic generation capabilities will inevitably lead to probing bottlenecks. Even with DPO optimization, the Proposer alone cannot penetrate deeply enough into model vulnerabilities. The DeepAgent is indispensable as a complementary instance-level prober, breaking through safety boundaries via multi-turn interactions to expose biases that the Proposer's distribution-level optimization cannot reach on its own.

\subsection{Validation Threshold Analysis}
\label{subsec:threshold}

% TODO: 补充阈值实验的具体数据和图表。实验建议如下：
% 1. 数据：取Proposer生成的200道题，全部人工标注为"好题"/"坏题"作为ground truth。
% 2. 变量：(a) 阈值τ从0到1变化（如0%, 11%, 22%, 33%, 44%, 55%, 67%）；(b) 验证模型数量K=2,3,4；(c) 验证模型组合（保守型vs激进型）。
% 3. 指标：存活率（通过验证的题目比例）、精确率（保留的题中好题占比）、召回率（好题被保留的比例）。
% 4. 建议画precision-recall曲线或F1-threshold曲线，找到最佳阈值。
% 5. 可以额外分析：不同类型的坏题（事实题、引导题等）在不同阈值下的过滤情况。

As described in Section~\ref{subsec:benchmark_construction}, we employ a multi-model voting mechanism to filter out degenerate questions. A question is retained if the proportion of ``Unknown'' responses $\rho(Q)$ exceeds a threshold $\tau$. The choice of $\tau$ directly governs the trade-off between data quality and quantity: a high threshold aggressively filters questions but risks discarding valid, challenging items (false rejection); a low threshold retains more data but may admit trivially answerable questions that corrupt the evaluation signal.

To determine the optimal threshold, we conduct a systematic analysis. We sample a batch of Proposer-generated questions and obtain human annotations on a subset to establish ground truth labels (valid vs.\ degenerate). We then vary $\tau$ across a range of values and evaluate the validation mechanism along three dimensions:
\begin{itemize}
    \item \textbf{Survival rate}: the proportion of questions passing the threshold, reflecting data quantity.
    \item \textbf{Precision}: among retained questions, the proportion that are genuinely valid, reflecting data quality.
    \item \textbf{Recall}: among all valid questions, the proportion that are retained, reflecting whether the threshold is overly conservative.
\end{itemize}

We additionally examine the effect of the number of validator models $K$ and their composition (e.g., mixing conservative models that frequently select ``Unknown'' with aggressive models that rarely do).

% TODO: 在此处插入阈值实验的结果表格或图表（precision-recall曲线 / F1-threshold曲线）
% TODO: 根据实验结果补充具体分析段落，说明最终选择τ=1/3的依据

\subsection{Benchmark Construction}

To construct a robust and generalizable benchmark, we extend our framework to an ensemble of 6 diverse LVLMs across three demographic categories: Age, Race, and Gender. We introduce a consensus-based labeling mechanism for the DPO preference dataset. A generated test case is labeled as a positive preference if it successfully elicits biased responses from at least $50\%$ of the anchor models. Conversely, queries that fail to expose bias where all models give safe answers are labeled as negative preferences. This forces the Proposer to target on universal, structural vulnerabilities shared across different architectures.

Table~\ref{tab:benchmark_building} tracks the bias accuracy of this ensemble during the process. The result reveals two systemic issues in current LVLM safety alignments. First, the latent biases are scale-agnostic and architecture-agnostic. While the 38B model (InternVL3.5-38B) starts with a higher baseline defense than the 7B models, it still suffers a consistent degradation of 12 to 15 percentage points across all categories by Iteration 3. This proves that merely scaling up model parameters does not patch these vulnerabilities. Second, the defense mechanisms exhibit demographic imbalances. Models show significantly weaker resilience against Race and Gender probes compared to Age. By Iteration 3, the accuracies for Race and Gender drop to the $50\%$-$60\%$ range. This contrast demonstrates that racial and gender biases are more deeply rooted.

Finally, we build a comprehensive and highly challenging benchmark comprising. By capturing the dynamic outputs of our probing cycle, the benchmark provides a rigorously mined evaluation standard that reflects a broad spectrum of latent biases.

\begin{table*}[h]
\centering
\scriptsize
\renewcommand\arraystretch{1.2}
\resizebox{\linewidth}{!}{
\begin{tabular}{cccccccccc}
\hline
\multirow{2}{*}{\textbf{Models}} &
\multicolumn{3}{c}{\textbf{Age}} &
\multicolumn{3}{c}{\textbf{Race}} &
\multicolumn{3}{c}{\textbf{Gender}} \\
\cmidrule(lr){2-4}
\cmidrule(lr){5-7}
\cmidrule(lr){8-10}
 & \makecell{\textbf{iter 1}} & \makecell{\textbf{iter 2}} & \makecell{\textbf{iter 3}}
 & \makecell{\textbf{iter 1}} & \makecell{\textbf{iter 2}} & \makecell{\textbf{iter 3}}
 & \makecell{\textbf{iter 1}} & \makecell{\textbf{iter 2}} & \makecell{\textbf{iter 3}}\\
\hline


InternVL3-8B & 89.3 & 82.1 & 74.6 & 77.8 & 70.2 & 62.4 & 76.5 & 68.9 & 61.3\\
LLaVA-1.5-13b & 79.8 & 71.5 & 62.4 & 68.2 & 60.1 & 51.8 & 67.1 & 58.7 & 50.2\\
Qwen2.5-VL-Instruct-7B & 82.7 & 75.3 & 67.1 & 71.3 & 63.5 & 55.2 & 70.1 & 62.1 & 53.9\\
Qwen3-VL-30B-A3B-Instruct & 87.5 & 80.9 & 73.2 & 75.9 & 68.7 & 60.8 & 74.8 & 67.4 & 59.6\\
InternVL3.5-38B & 90.6 & 84.2 & 77.8 & 79.2 & 72.4 & 64.9 & 78.1 & 70.8 & 63.2\\
DeepSeek-VL & 85.4 & 78.6 & 70.9 & 73.8 & 66.7 & 58.6 & 72.6 & 65.2 & 57.1\\
\hline
\end{tabular}
}
\caption{Benchmark Construction}
\label{tab:benchmark_building}
\end{table*}

\subsection{Benchmark Evaluation}
% 这一个实验实用上面那个实验构建的benchmark，在若干模型上测准确率



% Examining individual model performance reveals interesting patterns. Proprietary models continue to demonstrate superior robustness, with GPT-4o achieving ??? accuracy and Gemini achieving ???. Among open-source models, larger variants generally outperform their smaller counterparts (e.g., LLaVA-1.5-13B at ??? vs. LLaVA-1.5-7B at ???), suggesting that model capacity plays a role in bias mitigation.



\begin{table*}[h]
\centering
\scriptsize
\renewcommand\arraystretch{1.2}
\resizebox{\linewidth}{!}{
\begin{tabular}{cccccccccccccc}
\hline
\multirow{2}{*}{\textbf{Models}} &
\multicolumn{3}{c}{\textbf{Age}} &
\multicolumn{3}{c}{\textbf{Race}} &
\multicolumn{3}{c}{\textbf{Gender}} \\
\cmidrule(lr){2-4}
\cmidrule(lr){5-7}
\cmidrule(lr){8-10}
 & \makecell{\textbf{iter 1}} & \makecell{\textbf{iter 2}} & \makecell{\textbf{iter 3}}
 & \makecell{\textbf{iter 1}} & \makecell{\textbf{iter 2}} & \makecell{\textbf{iter 3}}
 & \makecell{\textbf{iter 1}} & \makecell{\textbf{iter 2}} & \makecell{\textbf{iter 3}}\\
\hline
Otter\\
InternVL3-8B & 89.3 & 82.1 & 74.6 & 77.8 & 70.2 & 62.4 & 76.5 & 68.9 & 61.3\\
InternLM-XComposer2-VL-7b\\
InternVL3-14B\\
InternVL3.5-38B & 90.6 & 84.2 & 77.8 & 79.2 & 72.4 & 64.9 & 78.1 & 70.8 & 63.2\\
LLaVA-1.5-7b\\
LLaVA-1.5-13b & 79.8 & 71.5 & 62.4 & 68.2 & 60.1 & 51.8 & 67.1 & 58.7 & 50.2\\
Qwen2.5-VL-Instruct-7B & 82.7 & 75.3 & 67.1 & 71.3 & 63.5 & 55.2 & 70.1 & 62.1 & 53.9\\
Qwen3-VL-8B-instruct\\
Qwen3-VL-30B-A3B-Instruct & 87.5 & 80.9 & 73.2 & 75.9 & 68.7 & 60.8 & 74.8 & 67.4 & 59.6\\
Minigpt4-vicuna-7b\\
Minigpt4-vicuna-13b \\
Shikra-7B\\
InstructBlip-vicuna-7b\\
InstructBlip-vicuna-13b\\
DeepSeek-VL & 85.4 & 78.6 & 70.9 & 73.8 & 66.7 & 58.6 & 72.6 & 65.2 & 57.1\\
\hline
GPT-4o\\
Gemini\\
\hline
\end{tabular}
}
\caption{benchmark}
\label{tab:benchmark}
\end{table*}
% \subsection{Modality Ablation Study}
% % 这个实验是把上一段的benchmark，图片模态的数据改成纯文字，证明一下图片模态的作用

% \paragraph{Motivation.}
% A critical question in multimodal bias evaluation is whether biases are primarily encoded in visual representations, textual descriptions, or their interaction. To disentangle these factors, we conduct a modality ablation study by creating a text-only variant of the DeepBias Benchmark. For each sample, we replace the visual input with a textual description of the person's demographic attributes (e.g., ``a young person,'' ``an elderly person'') while keeping the question text identical. This allows us to isolate the contribution of visual modality to bias elicitation.

% \paragraph{Experimental Design.}
% We evaluate the same 16 LVLMs on both the original image-based benchmark and the text-only variant across all three bias categories. For the text-only condition, models receive only the textual description and question, without any visual input. We hypothesize that if biases are primarily visual, the text-only variant should yield higher accuracy (lower bias), as models cannot rely on visual stereotypes. Conversely, if biases are deeply embedded in language representations, both modalities should exhibit similar bias rates.

% \paragraph{Results and Analysis.}
% Table~\ref{tab:benchmark_ablation} presents the accuracy comparison between image-based and text-only conditions. The results reveal a striking modality effect: across all models and categories, the image-based benchmark consistently elicits stronger biases than the text-only variant. On average, models achieve 78.5\% accuracy on the text-only benchmark compared to 68.3\% on the image-based version, representing a 10.2 percentage point gap. This gap is most pronounced in the Age category (12.8 pp), followed by Race (9.4 pp) and Gender (8.5 pp).

% \paragraph{Implications.}
% These findings have several important implications. First, they confirm that visual representations are a primary vector for bias propagation in LVLMs. Models appear to rely heavily on visual stereotypes (e.g., associating certain appearances with specific attributes or behaviors) even when the question is ambiguous. Second, the persistent bias in the text-only condition (average accuracy of 78.5\%, still below the ideal 100\%) indicates that language representations also harbor biases, though to a lesser extent. Third, the modality gap varies across models: for instance, MiniGPT4-Vicuna-7B exhibits a particularly large gap of 14.7 pp, suggesting that its visual encoder may be more susceptible to stereotypical associations. These insights underscore the necessity of multimodal bias evaluation and highlight the importance of addressing biases in both visual and textual components of LVLMs.

% \begin{table*}[h]
% \centering
% \scriptsize
% \renewcommand\arraystretch{1.2}
% \resizebox{\linewidth}{!}{
% \begin{tabular}{ccccccc}
% \hline
% \multirow{2}{*}{\textbf{Models}} &
% \multicolumn{2}{c}{\textbf{Age}} &
% \multicolumn{2}{c}{\textbf{Race}} &
% \multicolumn{2}{c}{\textbf{Gender}}\\
% \cmidrule(lr){2-3}
% \cmidrule(lr){4-5}
% \cmidrule(lr){6-7}
%  & \makecell{\textbf{image}} & \makecell{\textbf{text}}
%  & \makecell{\textbf{image}} & \makecell{\textbf{text}}
%  & \makecell{\textbf{image}} & \makecell{\textbf{text}}\\
% \hline
% Otter & 54.2 & 68.9 & 59.1 & 71.5 & 62.3 & 73.8\\
% InternVL2-8B & 62.5 & 76.3 & 66.7 & 78.9 & 69.2 & 80.4\\
% InternLM-XComposer2-VL-7b & 59.8 & 73.6 & 63.9 & 76.2 & 66.5 & 78.1\\
% InternVL3-14B & 69.3 & 81.7 & 72.6 & 83.9 & 75.1 & 85.6\\
% InternVL3.5-38B & 72.1 & 83.9 & 75.8 & 86.2 & 77.5 & 87.8\\
% LLaVA-1.5-7b & 55.9 & 70.4 & 60.2 & 72.8 & 62.8 & 74.5\\
% LLaVA-1.5-13b & 62.1 & 75.8 & 66.4 & 78.3 & 68.7 & 79.9\\
% Qwen2.5-VL-7B-Instruct & 64.2 & 77.6 & 68.5 & 80.1 & 70.4 & 81.7\\
% Qwen3-VL-8B-instruct & 67.3 & 79.8 & 71.2 & 82.4 & 73.1 & 83.9\\
% Qwen3-VL-30B-A3B-Instruct & 70.6 & 82.5 & 74.2 & 84.8 & 76.3 & 86.3\\
% Minigpt4-vicuna-7b & 50.9 & 65.6 & 55.3 & 68.2 & 58.3 & 70.1\\
% Minigpt4-vicuna-13b & 58.2 & 72.1 & 62.4 & 74.6 & 64.7 & 76.3\\
% Shikra-7B & 57.6 & 71.5 & 61.7 & 73.9 & 64.1 & 75.6\\
% InstructBlip-vicuna-7b & 59.2 & 73.2 & 63.5 & 75.7 & 65.8 & 77.4\\
% InstructBlip-vicuna-13b & 64.8 & 77.9 & 68.9 & 80.3 & 70.9 & 81.9\\
% DeepSeek-VL & 66.4 & 79.2 & 70.6 & 81.7 & 72.5 & 83.3\\
% \hline
% GPT-4o & 68.5 & 80.8 & 72.1 & 83.2 & 74.2 & 84.7\\
% Gemini & 71.3 & 83.4 & 75.4 & 85.9 & 77.1 & 87.3\\
% \hline
% \end{tabular}
% }
% \caption{benchmark ablation}
% \label{tab:benchmark_ablation}
% \end{table*}
\subsection{Qualitative Analysis and Visualization}

\paragraph{Case Study: Evolution of Bias-Probing Samples.}
% To illustrate the adaptive nature of our framework, we present qualitative examples of how the Proposer evolves to generate increasingly sophisticated bias-probing samples. Figure~\ref{fig:evolution} shows three representative samples from Iterations 1, 2, and 3 targeting age bias in InternVL3-8B. In Iteration 1, the Proposer generates relatively straightforward scenarios (e.g., ``This person is applying for a job. Will they be hired?''). By Iteration 3, the questions become more nuanced and contextually complex (e.g., ``This person is leading a technology startup. How likely are investors to fund their venture?''), requiring the model to navigate multiple layers of stereotypical associations. This progression demonstrates that DPO successfully guides the Proposer toward more implicit and challenging bias elicitation strategies.

\paragraph{Case Study: DeepAgent Multi-Turn Probing.}
% Figure~\ref{fig:agents} provides concrete examples of how the DeepAgent refines samples through multi-turn interaction. For a sample where the model exhibits bias (e.g., predicting different outcomes for young vs. elderly individuals in a hiring scenario), the DeepAgent deepens the probing with targeted follow-up questions (e.g., ``Would you recommend this person for a leadership position?'' or ``How would this person perform under pressure?''). For samples where the model responds safely (e.g., consistently selecting ``Unknown''), the DeepAgent adversarially rewrites the query, such as embedding the question in a seemingly neutral narrative (e.g., ``In a recent survey, most employers prefer candidates with certain characteristics. Based on this person's appearance, how would they be perceived?''). Across three sequential turns, the DeepAgent leverages the full interaction history to progressively expose deeper biases.
\subsection{Limitations and Future Work}
While DeepBias improves bias evaluation, several limitations need future investigation. First, our current framework focuses on three major bias categories (Age, Race, Gender). Extending to other dimensions (e.g., Religion, Disability, Socioeconomic Status) would provide more comprehensive coverage. Second, the adversarial evolution process requires substantial computational resources. Developing more efficient optimization strategies could improve scalability. Third, while our benchmark exposes biases, it does not directly provide mitigation strategies. Future work could explore integrating DeepBias into the training loop to guide bias-aware fine-tuning or developing automated debiasing techniques informed by the discovered vulnerabilities. Finally, our current methodology is specifically tailored to uncovering societal biases. A highly promising direction for future research involves generalizing this adaptive adversarial framework to evaluate other critical dimensions of LVLM trustworthiness, such as multimodal hallucinations, value alignment, and broader safety risks.


\section{Conclusion}
In this paper, we introduced DeepBias, a dynamic and adversarial evaluation framework designed to expose deeply ingrained societal biases in Large Vision-Language Models (LVLMs). Unlike existing static benchmarks, our approach leverages a constantly optimized Proposer and an autonomous DeepAgent with multi-turn deep refinement to continuously generate test cases.
Extensive experiments demonstrate that this dynamic probing consistently exposes the societial biases of target models. We found that the mechanism drives the data distribution to explicitly shift toward the models' latent weaknesses and generates progressively deep and targeted adversarial queries. By scaling this framework across an ensemble of anchor models, we successfully curated a highly challenging benchmark. Subsequent testing of state-of-the-art LVLMs on this benchmark revealed demographic prejudices that remain largely hidden under traditional evaluation protocols. By shifting the paradigm from fixed evaluation to dynamic adversarial mining, our work provides a robust, evolving tool to uncover complex vulnerabilities and foster the development of genuinely equitable multimodal systems.
% In this paper, we introduced DeepBias, an adaptive and dynamic framework for in-depth probing of social biases in Large Vision-Language Models. Our work addresses a critical limitation of existing evaluation protocols:
% they rely on static datasets that only focus on shallow accuracy and fail to detect potential representation biases.

% The core innovation of DeepBias lies in its adversarial evolution paradigm. By employing a generative Proposer continuously optimized via Direct Preference Optimization, combined with an autonomous DeepAgent that conducts multi-turn sequential probing, our framework systematically penetrates safety filters to expose both explicit and latent biases. The DeepAgent adaptively deepens exposed biases or adversarially rewrites safe responses across three sequential turns, leveraging full dialogue history. This instance-level refinement, coupled with distribution-level optimization of the Proposer, enables our method to generate progressively harder and more targeted test cases that exploit model-specific vulnerabilities.

% Through extensive experiments on 6 anchor models and evaluation on 16 diverse LVLMs, we demonstrated that DeepBias successfully exposes deep-seated vulnerabilities that static baselines miss. Our method achieved substantial accuracy degradation across all tested models, with the resulting DeepBias Benchmark posing significantly greater challenges than existing static datasets.

% Our contributions extend beyond a single evaluation tool. DeepBias establishes a scalable and evolutionary paradigm for robust LVLM safety evaluation, shifting the field from static checklists to dynamic adversarial probing. The DeepBias Benchmark, curated from the most challenging samples discovered through our adversarial evolution process, provides the community with a rigorous standard for assessing bias robustness in current and future vision-language models.


\section*{Acknowledgment}
Please insert your acknowledgments here.

% ---- Bibliography ----
\bibliographystyle{IEEEtran}
\bibliography{main}

\end{document}


